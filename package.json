{
  "private": true,
  "scripts": {
    "markdownlint:check": "markdownlint-cli2 README.md",
    "prettier:check": "prettier --check README.md package.json .prettierrc.yaml .github/**/*.yaml",
    "prettier:format": "prettier --write README.md package.json .prettierrc.yaml .github/**/*.yaml"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.20.0",
    "prettier": "^3.0.0"
  }
}
